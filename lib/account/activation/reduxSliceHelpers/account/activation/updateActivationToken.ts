// TODO: Invoke the appropriate api mutation for each function. Update the Redux states for each step of the function.

const updateActivationToken = {
  activate: (/*userId: string*/) => {
    return;
  },
  regen: (/*userId: string*/) => {
    return;
  }
};

export default updateActivationToken;
