// TODO: Invoke the appropriate api mutation for each function. Update the Redux states for each step of the function.
const getActivationToken = (/*userId: string*/): void => {
  // Fetch the user's activation token.
  return;
};

export default getActivationToken;
